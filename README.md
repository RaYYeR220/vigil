# Vigil

An autonomous on-chain risk guardian that never executes an unsafe action.
Watch → gate → execute → observe, on top of KeeperHub.

## Packages
- `packages/guard-core` — the policy + simulation interlock (pure, deterministic).

## Develop
```bash
pnpm install
pnpm test
```
